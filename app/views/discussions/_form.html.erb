<%= simple_form_for [@project, @discussion], html: { class: "form-horizontal" } do |f| %>
	<%= f.input :title %>
	<br>
	<%= f.input :description %>
	<br>
	<%= f.submit class: "btn btn-primary" %>
	<br>
<% end %>