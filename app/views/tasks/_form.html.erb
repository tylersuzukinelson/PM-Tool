<%= simple_form_for [@project, @task], html: { class: "form-horizontal" } do |f| %>
	<div class="container">
		<%= f.input :title %>
		<br>
		<%= f.input :due_date %>
		<br>
		<%= f.input :body %>
		<%= f.submit "Add to Project", class: "btn btn-submit" %>
	</div>
<% end %>