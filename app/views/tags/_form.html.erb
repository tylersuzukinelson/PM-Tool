<div class="container">
	<%= simple_form_for [@project, @tag], html: { class: "form-horizontal" } do |f| %>
		<%= f.input :name, label: 'Add Tags (separated by spaces)' %><%= f.submit class: "btn btn-primary" %>
		<br>
	<% end %>
</div>