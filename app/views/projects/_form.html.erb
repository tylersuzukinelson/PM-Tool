<%= simple_form_for @project, html: {class: "form-horizontal"} do |f| %>
	<div class="container">
		<%= f.input :title %>
		<br>
		
		<%= f.input :due_date %>
		<br>

		<%= f.input :description %>
		<br>

		<%= f.submit class: "btn btn-submit" %>
	</div>
<% end %>